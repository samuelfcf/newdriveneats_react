import React from "react";

const Footer = () => {
  return (
    <footer>
      <button className="finalize-order-button">Selecione os 3 itens para fechar o pedido</button>
    </footer>
  );
}

export { Footer }